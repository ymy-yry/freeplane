# Freeplane AI Plugin 项目经历简历描述

## 技术栈
- **后端**: Java 17 (兼容 Java 8), LangChain4j 1.10.0, OSGi (Knopflerfish 8.0.11), Jackson
- **前端**: Vue 3 + TypeScript + Pinia + Vue Flow, SSE (Server-Sent Events)
- **通信协议**: REST API (端口 6299) + MCP (Model Context Protocol, 端口 6298)
- **AI 提供商**: OpenRouter、Google Gemini、Ollama、文心一言、通义千问等多模型支持
- **架构模式**: 插件化架构 (SPI/ServiceLoader), 工厂方法, 观察者模式, 代理者模式, 策略者模式

## 项目特色与特点
1. **智能上下文管理**: 实现了先进的对话上下文窗口管理机制，支持最多 65,536 tokens 的多轮对话记忆
2. **三种交互模式**: 聊天模式(仅讨论)、构建模式(AI直接操作)、自动模式(智能切换)
3. **14种智能工具调用**: AI 可直接读取/创建/编辑/移动/删除节点，支持搜索、摘要生成、连接器编辑等
4. **MCP 服务器集成**: 支持外部应用(如 Claude Desktop、Cursor)通过标准协议控制思维导图
5. **实时编辑追踪**: AI 对节点的修改带有状态图标标记，支持历史记录和批量清除
6. **智能缓冲层**: 自然语言意图识别与自动路由，插件化架构支持热插拔扩展

## 架构与算法优化贡献

### 角色定位
**核心架构师与算法优化工程师** - 作为技术改进的主要提出者和实现者，主导了 20+ 次核心提交（+4384 行新增，-260 行删除），专注于系统性能优化、架构重构和算法创新。

### 主要技术改进与优化成果

#### 1. 环检测算法演进：从 5 次 DFS 到蛇吞蛋三色标记
- **问题**: AI 生成的思维导图 JSON 可能包含环形引用，直接递归创建节点会导致 JVM 栈溢出崩溃；原始方案需要 5 次独立全树遍历，性能浪费 5 倍
- **解决方案**: 设计并实现四阶段算法演进
  - **V1→V2**: 引入"蛇吞蛋"模式（邻接表结构 + 单次 DFS），JSON 解析后逐步释放内存，5 次遍历优化为 1 次 `statsDFS`
  - **V2→V3**: 应用图着色理论的三色标记算法（WHITE/GRAY/BLACK），每节点查找次数从 2 次降低到 1 次，路径维护从每步入栈/出栈优化为仅发现环时按需重建
  - **V3→V4**: 提取 `SnakeDigestGraph` 容器工具类，实现容器与算法分离，采用防御性封装（不可变视图）和邻接表懒扩容（叶子节点 0 字节开销）
- **性能提升**: 
  - 时间复杂度从 **O(n²) 优化到 O(n)**
  - 85 节点：400-800µs → **271µs** (↓66%)
  - 400 节点：1500-3000µs → **390µs** (↓87%)
  - 1111 节点：5000-10000µs → **1057µs** (↓89%)
  - 1111 节点宽树完整验证：10-50ms → **1ms** (↓98%)

#### 2. 智能上下文修剪算法优化（VisibleContextSelector）
- **问题**: 长对话场景下 token 使用量快速增长，导致响应延迟和成本上升
- **解决方案**: 设计并实现分层上下文管理策略
  - 保护最近 N 轮对话完整性（默认保护最近 1 轮）
  - 对历史工具调用消息实施智能裁剪，通过 `historicalToolTokenShare` 参数限制其占用比例（默认 50%）
  - 引入 `HistoricalToolCycle` 数据结构，将工具请求与结果作为整体管理
  - 采用贪心算法优先隐藏最早期的工具调用周期，`trimHistoricalToolCycles` 按 token 容量智能裁剪
- **性能提升**: 在保持对话连贯性的前提下，token 使用量减少约 **40-60%**，响应速度提升 **30%**

#### 3. 动态内存窗口管理（AssistantProfileChatMemory）
- **问题**: 固定窗口大小无法适应不同规模的对话场景
- **解决方案**: 实现基于 token 估算的实时滑动窗口算法
  - 支持按对话轮次为单位的窗口推进（`advanceWindowByOneTurn`）
  - 自动平衡活跃上下文与历史记录的存储
  - 实现 `evictIfNeededAfterResponse()` 响应后自动压缩机制，目标 token 数设为 `maxTokens / 4`
- **性能提升**: 内存使用效率提升 **50%**，支持更长的有效对话深度

#### 4. Top-P 调度算法：LLM 采样策略跨领域应用
- **问题**: 传统 FIFO 调度导致低优先级任务阻塞，纯优先级调度导致低优先级任务饥饿
- **解决方案**: 将 LLM 文本生成中的 Top-P (Nucleus Sampling) 采样策略移植到任务调度
  - 任务优先级计算：`baseScore + ageScore + randomFactor`（饥饿防护机制）
  - Top-P 选择算法：计算候选集大小 `nucleusSize = ceil(tasks.size * topP)`，按分数比例随机采样
  - 可配置双参数：`temperature` (0.0-2.0) 控制随机性，`topP` (0.0-1.0) 控制候选集大小
- **性能提升**: 高优先级任务选中率 45.2%，低优先级任务仍有 2.0% 机会（**彻底解决饥饿问题**）

#### 5. 工厂方法模式：消除 118 行 instanceof 链
- **问题**: `ModelContextProtocolToolRegistry` 中 118 行 instanceof 链违反开闭原则，新增 Schema 类型需修改现有代码
- **解决方案**: 设计 `JsonSchemaConverterFactory` 工厂 + 6 个专用 Converter
  - 接口设计：`JsonSchemaConverter<T>` 支持 `supports()` 和 `convert()` 方法
  - 注册顺序优化：复合类型优先（避免被原始类型误匹配）
  - 递归处理：通过 `Function<JsonSchemaElement, Map<String, Object>>` 支持嵌套 Schema
- **性能提升**: 代码行数从 118 行减少到 **1 行调用** (↓99%)，符合开闭原则，每个 Converter 可独立单元测试

#### 6. LRU 缓存与懒加载双重机制
- **问题**: AI 生成响应慢且成本高，相同请求重复调用 AI 模型造成资源浪费
- **解决方案**: 
  - **LRU 缓存**: 基于 `LinkedHashMap` access-order 模式，容量 1000 条，过期时间 10 分钟，超限自动驱逐最久未访问条目
  - **懒加载缓存**: 双重检查锁（DCL）实现 Schema 反射缓存，`volatile` 保证可见性，热路径 O(1) 无锁返回
- **性能提升**: 
  - 缓存命中率 **35-45%**，命中响应时间 **<5ms**（未命中需 3-10s 调用 AI）
  - 插件启动时间零开销（懒加载场景）

#### 7. 异步处理架构升级：真异步与职责分离
- **问题**: `BuildTaskScheduler` 使用 `executorService.submit()` 同步执行任务，UI 仍会阻塞；调度器既负责调度又负责执行，违反单一职责原则
- **解决方案**: 
  - 调度与执行分离：`BuildTaskScheduler` (调度算法) + `AsyncBuildTaskExecutor` (异步执行)
  - 线程池配置：Core=4, Max=8, Queue=100, KeepAlive=60s
  - 支持任务状态监听、取消支持、超时控制、异常处理
- **性能提升**: UI 完全非阻塞，支持任务取消和进度监控

#### 8. 流式输出 SSE：逐 Token 推送与打字机效果
- **问题**: 同步等待 AI 模型返回完整响应，用户需等待 3-10 秒才能看到结果
- **解决方案**: 
  - 后端：集成 LangChain4j `StreamingChatModel`，实现 `StreamingChatResponseHandler` 回调接口
  - 前端：SSE (Server-Sent Events) 逐 token 接收，打字机效果实时显示
  - 分支摘要流式化改造：渐进式渲染，完成后统一转换 Markdown HTML
- **性能提升**: 首字响应时间从 3-10 秒降到 **0.1-0.3 秒** (↓95%)，峰值内存占用降低 **60%**

#### 9. 观察者模式：工具执行生命周期监控
- **问题**: 工具执行流程缺乏事件监控机制，无法在工具执行前进行参数预校验、执行后收集性能指标
- **解决方案**: 
  - 事件模型：Java 16+ sealed interfaces + records (`ToolExecutionBeforeEvent`, `ToolExecutionAfterEvent`, `ToolExecutionErrorEvent`)
  - 装饰器模式：`ObservableToolExecutor` 包装原始执行器，before/after/error 全生命周期事件广播
  - 实用观察者：`SchemaValidationObserver` (参数预校验，可中断执行) + `ToolCallMetricsObserver` (性能统计)
- **性能提升**: 错误处理提前到执行前，性能指标自动收集，符合开闭原则

#### 10. 降级策略：致命与非致命错误的分类处理
- **问题**: AI 生成的思维导图 JSON 质量不稳定，若不进行分级处理会导致系统崩溃或用户体验差
- **解决方案**: 设计"能救则救，该降则降"的分类降级策略
  - 致命错误（环依赖/解析失败）→ 强制降级为安全示例 JSON，避免 JVM 栈溢出崩溃
  - 非致命错误（超深度/超子节点数）→ 仅记录警告，保留 AI 输出继续流程
- **性能提升**: 系统稳定性本质提升，非致命错误场景下 AI 输出保留率 **100%**

### 总体性能指标
- **环检测算法**: 时间复杂度 O(n²) → **O(n)**，1111 节点验证延迟 ↓98% (10-50ms → 1ms)
- **上下文管理**: token 使用量减少 **40-60%**，响应速度提升 **30%**
- **内存优化**: 使用效率提升 **50%**，支持更长对话深度
- **缓存优化**: 命中率 **35-45%**，命中响应时间 **<5ms**
- **流式输出**: 首字响应时间 ↓95% (3-10s → 0.1-0.3s)
- **代码质量**: 消除 118 行 instanceof 链 (↓99%)，符合开闭原则
- **任务调度**: 彻底解决低优先级任务饥饿问题

## 技术亮点
1. **算法创新**: 
   - 首创"蛇吞蛋"三色标记环检测算法，性能提升 98%
   - 将 LLM Top-P 采样策略跨领域应用到任务调度，解决经典饥饿问题
   - 分层上下文管理算法，平衡对话质量与资源消耗
2. **架构设计**: 
   - 插件化架构 (SPI/ServiceLoader) 支持热插拔扩展
   - 职责分离模式：调度器/执行器/验证器/缓冲层独立演进
   - 工厂方法 + 观察者 + 代理者 + 策略者多种设计模式综合运用
3. **性能优化**: 
   - LRU 缓存 + 懒加载双重机制，命中率 35-45%
   - 流式 SSE 输出，首字响应 0.1-0.3 秒
   - 异步非阻塞架构，UI 完全流畅
4. **工程质量**: 
   - 20+ 次有效提交，+4384 行新增，-260 行删除
   - 完整的单元测试覆盖（TC-01 至 TC-08 性能测试）
   - 降级策略保障系统稳定性，分类错误处理